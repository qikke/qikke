{
  "apps": [
    {
      "name": "qikke",
      "script": "./server/server.js",
      "env_production": {
        "NODE_ENV": "production",
        "HOST": "localhost"
      }
    }
  ],
  "deploy": {
    "production": {
      "user": "qk",
      "host": "qikke.cn",
      "repo": "git@github.com:qikke/qikke.git",
      "ref": "origin/master",
      "path": "/home/qk/project/qikke",
      "post-deploy": "npm install && npm run build && pm2 startOrRestart ecosystem.json --env production"
    }
  }
}
